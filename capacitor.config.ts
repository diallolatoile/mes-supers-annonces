import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mes-supers-annonces',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
